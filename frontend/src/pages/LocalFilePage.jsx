import React, { useEffect, useRef, useState } from "react";
import io from "socket.io-client";

const LocalFilePage = ({ socketUrl }) => {
  

  return (
    <div>Local file player</div>
  );
};

export default LocalFilePage;
